package com.example.myfleet;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class HomeActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.create_account_page);

        // Adicione aqui o código para configurar a interface da tela inicial após o login
        // Por exemplo, você pode exibir um painel de controle, lista de itens, etc.
    }
}
